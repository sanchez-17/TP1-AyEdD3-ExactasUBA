#include <iostream>
#include <vector>
#include <utility>
#include <algorithm>
using namespace std;
int N, M;

void solve(){
    
}

int main() {
    int C;
    cin >> C;
    for(int c = 0; c < C; c++){
        cin >> N >> M;
        for(int m = 0; m < M; m++){
            int u, v, d, r;
            cin >> u >> v >> d >> r;
        }
        solve();
    }
    return 0;
}

